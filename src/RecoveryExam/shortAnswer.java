package RecoveryExam;

public class shortAnswer {
//explain constructor briefly
//-Constructor is a special method called every time whenever object is created
//diffirent bn int and long
//int is 32 bits(4bytes)
//explain 'class' briefly
//class is representaion of bojects and it is used  to represent real life template on java
//Find 

	// . str = str + word; //every time when we use concatenation it takes more
	// space memory
//	tempStringBuffer.append(word)//it concatenate in the same memory space.

	// str = str + word; //every time when we use concatenation it takes more space
	// memory
	// tempStringBuffer.append(word)//it concatenate in the same memory space.

	// Pricedent () - + / % ---(+ -)
}
